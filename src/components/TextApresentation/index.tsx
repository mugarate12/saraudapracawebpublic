import React from 'react'

import * as Styled from './styles'

const TextApresentation:React.FC = ({ children }) => {
  return <Styled.Text>{children}</Styled.Text>
}

export default TextApresentation
